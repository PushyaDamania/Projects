<!DOCTYPE html>
<html lang="en">
{{> fragments/html_header}}

<body class="cream-bg">

{{> fragments/navbar }}

<div class="bg-circle navy top-left"></div>
<div class="bg-circle maroon bottom-right"></div>

<div class="container" style="margin-top: 40px;">

    <div class="page-header">
        <h2>Welcome to HCI Insights</h2>
        {{#loggedInUser}}
            <p class="page-subtitle">Hi, <strong>{{username}}</strong>. Your HCI research dashboard is ready.</p>
        {{/loggedInUser}}
    </div>

    {{#errorMessage}}
        <p class="error-message">{{.}}</p>
    {{/errorMessage}}

    <div class="center-align" style="margin-top: 30px;">
        <a class="btn" href="/studies/create">Create New Study</a>
    </div>

    <hr style="margin: 40px 0;">

    <div>
        <h3>Your Recent Studies</h3>

        {{#studies}}
            <div class="card">
                <h4 style="margin-bottom: 6px;">{{title}}</h4>

                <p>{{description}}</p>

                <p><strong>Platform:</strong> {{platform}}</p>
                <p><strong>Status:</strong> {{status}}</p>

                <div class="card-actions">
                    <div class="card-action-left">
                        <a class="btn small" href="/studies/{{studyId}}">View</a>
                    </div>
                </div>
            </div>
        {{/studies}}

        {{^studies}}
            <p>You haven’t created any studies yet. Start by creating one!</p>
        {{/studies}}
    </div>

</div>

{{> fragments/footer}}
</body>
</html>